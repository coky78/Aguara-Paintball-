import { createClient } from "@supabase/supabase-js";
const SLOTS={home_media_1:20,home_media_2:21,home_media_3:22,home_media_4:23,home_media_5:24,home_media_6:25};
export default async function handler(req,res){try{const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return res.status(500).json({ok:false,message:"Faltan variables de Supabase."});const supabase=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const {data,error}=await supabase.from("media_library").select("slot_key,media_type,public_url,title,alt_text,enabled,position_x,position_y,zoom,sort_order").in("slot_key",Object.keys(SLOTS)).eq("enabled",true).order("sort_order",{ascending:true});if(error)throw error;return res.status(200).json({ok:true,media:data||[]});}catch(error){console.error("PUBLIC HOME MEDIA ERROR:",error);return res.status(500).json({ok:false,message:error.message||"Error cargando medios de portada."});}}
